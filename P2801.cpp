#include <iostream>
#include <stdio.h>
#include <math.h>
using namespace std;
int n, q, high[1000001], blen, tmpx, tmpy, tmpz;
int blocks[10001], belong[10001];
char tmpc;
int main() {
	cin >> n >> q, blen= sqrt(n);
	for(int i= 1; i <= n; i++) cin >> high[i];
    for(int i= 1; i <= n; i++) {
    }
    while(q--) {
        cin >> tmpc >> tmpx >> tmpy >> tmpz;
    }
	return 0;
}